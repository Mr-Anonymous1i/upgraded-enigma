#Number Guesser
#1/9/2022
#User tries guess numbers which are randomly generated by the program

import random

random_number = random.randint(1, 10)
guess = int(input("""Guess a number between 1 and 10: 
"""))
turn = 4

while random_number != guess:
    guess = int(input("""Guess again! 
"""))
    turn -= 1
    if turn == 0:
        print("Unfortunately you have failed! Try again!")
        break
else:
    if guess == random_number:
        print("Good guess!")